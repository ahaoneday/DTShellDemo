# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DTShellDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DTShellDemo

  target 'DTShellDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DTShellDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod 'DTRoutCategry', :path => 'Lib/DTRoutCategry'

end
